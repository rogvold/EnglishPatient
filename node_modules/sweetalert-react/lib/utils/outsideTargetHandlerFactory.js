'use strict';

exports.__esModule = true;
exports['default'] = outsideTargetHandlerFactory;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _isSourceFound = require('./isSourceFound');

var _isSourceFound2 = _interopRequireDefault(_isSourceFound);

function outsideTargetHandlerFactory(localNode, eventHandler) {
  return function (evt) {
    evt.stopPropagation();
    var source = evt.target;
    var found = false;
    while (source.parentNode) {
      found = _isSourceFound2['default'](source, localNode);
      if (found) return;
      source = source.parentNode;
    }
    eventHandler(evt);
  };
}

module.exports = exports['default'];